<Page
    x:Class="Hack4Europe.View.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:userControls="using:Hack4Europe.UserControls"
    xmlns:local="using:Hack4Europe.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <userControls:MainViewGridControl TopIconLogoUri="/CommonAssets/Logos/logo.png" TopImageUri="/CommonAssets/Logos/sugar_mole.png">
       <userControls:MainViewGridControl.Children>
            <ListBox ItemsSource="{Binding DomoItemsList, Mode=TwoWay}">
                <ListBox.DataContext>
                    <local:HomeViewModel></local:HomeViewModel>
                </ListBox.DataContext>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <userControls:DomoItemControl/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </userControls:MainViewGridControl.Children>
    </userControls:MainViewGridControl>

</Page>
